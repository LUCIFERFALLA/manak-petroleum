# OS / editor
.DS_Store
Thumbs.db
desktop.ini
# scratch
media_tmp/
*.log
# agent tooling (local only — must NOT deploy to the web host)
.agents/
.claude/
skills-lock.json
node_modules/
# secrets / local env (never deploy)
.env
.env.*
# AI agent working files (upload to ElevenLabs, don't deploy as web pages)
ai-agent/
